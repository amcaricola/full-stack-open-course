sequenceDiagram
participant user
participant browser
participant server

    user->>browser: enters in the web

    browser->>server: does a GET request to https://studies.cs.helsinki.fi/exampleapp/ps://studies.cs.helsinki.fi/exampleapp/spa

    activate server
    server-->>browser: sends the HTML document
    deactivate server

    browser->>server: does a GET request to https://studies.cs.helsinki.fi/exampleapp/main.css

    activate server
    server-->>browser: sends the CSS file with styles
    deactivate server

    browser->>server: does a GET request to https://studies.cs.helsinki.fi/exampleapp/spa.js

    activate server
    server-->>browser: sends a JavaScript file that manages the SPA
    deactivate server

    Note right of browser: The browser executes the JavaScript code from the spa.js

    browser->>server: does GET request to https://studies.cs.helsinki.fi/exampleapp/data.json

    activate server
    server-->>browser: sends a json file with all notes [{ "content": "the content", "date": "2026-02-17" }, ... ]
    deactivate server

    Note right of browser: spa.js handles the content and then tells the browser to render it

    browser-->>user: renders the page with all the content


      Note right of user:Now user can see all the page content
